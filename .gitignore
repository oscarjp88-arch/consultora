credentials.json
.env
node_modules/
token.json
creditek/workers/gemini-proxy/.claude/
.mcp.json
.claude/
credenciales_admins_*.csv
node_modules/

# Backups locales generados por `sed -i.bak` de scripts de migración.
# Los archivos "reales" .bak no van al repo; si algún .bak legítimo hiciera falta versionar, agrégalo con `git add -f`.
*.bak
